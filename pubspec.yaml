name: collection_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  csv: ^5.0.2
  path_provider: ^2.0.15
  permission_handler: ^11.0.1
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  intl: ^0.19.0          # Updated to latest version for localization
  flutter_localizations: # Required for i18n
    sdk: flutter
    

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
     